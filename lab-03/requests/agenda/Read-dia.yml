info:
  name: Read-dia
  type: http

http:
  method: GET
  url: http://localhost:8080/api/dia
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
