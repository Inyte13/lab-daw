info:
  name: Create-bloque
  type: http

http:
  method: POST
  url: http://localhost:8080/api/2026-05-10/11:30
  body:
    type: json
    data: |
      {
        "descripcion": "# Nuevo Bloque\nEste es un bloque de prueba creado desde Bruno."
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​