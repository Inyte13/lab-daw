info:
  name: Update-bloque
  type: http

http:
  method: PUT
  url: http://localhost:8080/api/2026-05-10/11:30
  body:
    type: json
    data: |
      {
        "descripcion": "# Bloque Actualizado\nAcabo de editar este archivo a través de la API."
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
